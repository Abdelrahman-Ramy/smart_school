import 'package:flutter/material.dart';

class ParentNotificationsView extends StatelessWidget {
  const ParentNotificationsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ParentNotificationsView'),
      ),
    );
  }
}