import 'package:flutter/material.dart';

class TeacherChatView extends StatelessWidget {
  const TeacherChatView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('TeacherChatView')));
  }
}
