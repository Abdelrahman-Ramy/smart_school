import 'package:flutter/material.dart';

class StudentNotificationsView extends StatelessWidget {
  const StudentNotificationsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('StudentNotificationsView'),
      ),
    );
  }
}